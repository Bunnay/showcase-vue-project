import type { Category, CreateCategory, UpdateCategory } from "category";

export const DEFAULT_CATEGORY_DATA: Category = {
  id: -1,
  name: "",
  image: "",
};

export const DEFAULT_CREATE_CATEGORY: CreateCategory = {
  name: "",
  image: "",
};

export const DEFAULT_UPDATE_CATEGORY: UpdateCategory = {
  name: "",
  image: "",
};
